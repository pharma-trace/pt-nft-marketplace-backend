let Nft;_c59‍.x([["createNFT",()=>createNFT],["userNft",()=>userNft],["collectionName",()=>collectionName],["findUser",()=>findUser],["searchUser",()=>searchUser],["likeNft",()=>likeNft],["unlikeNft",()=>unlikeNft],["usersNft",()=>usersNft]]);_c59‍.w("../models/nft",[["default",["Nft"],function(v){Nft=v}]]);

       const createNFT = async(req, res) => {
    const {
        ipfs,
        name,
        description,
        price,
        category,
        properties,
        supply,
        wallet,
        protocol,
        metadata,
        collectionName,
        collectionImage,
        collectionDescription,
    } = req.body;
    if (!name.length && !description.length) {
        return res.json({
            error: "NFT Asset Name and Description Required",
        });
    }
    try {
        const nft = new Nft({
            ipfs,
            name,
            description,
            category,
            price,
            properties,
            supply,
            wallet,
            collectionName,
            collectionImage,
            collectionDescription,
        });
        nft.save();
        res.json(nft);
    } catch (err) {
        _c59‍.g.console.log(err);
        return res.status(400).send("Error Found");
    }
};

       const userNft = async(req, res) => {
    try {
        const nft = await Nft.findById(req.params._id);
        res.json(nft);
    } catch (err) {
        _c59‍.g.console.log(err);
    }
};

       const collectionName = async(req, res) => {
    try {
        const nft = await Nft.findOne(req.params.collectionName);
        res.json(nft);
    } catch (err) {
        _c59‍.g.console.log(err);
    }
};

       const findUser = async(req, res) => {
    try {
        const nft = await Nft.findById(req.auth._id);
        //usefollowing list
        let following = Nft.following;
        following.push(nft._id);
        const people = await Nft.find({ _id: { $nin: following } })
            .select("name wallet")
            .limit(10);
        res.json(nft);
    } catch (err) {
        _c59‍.g.console.log(err);
    }
};

       const searchUser = async(req, res) => {
    const { query } = req.params;
    if (!query) return;
    //regex is a special method for mongodb
    // The i modifier is used to perform case-insensitive matching
    try {
        const nft = await Nft.find({
            $or: [{ name: { $regex: query, $options: "i" } }],
        }).select("wallet");
        res.json(profile);
    } catch (err) {
        _c59‍.g.console.log(err);
    }
};

       const likeNft = async(req, res) => {
    try {
        const nft = await Nft.findByIdAndUpdate(
            req.body._id, {
                $addToSet: {
                    likes: req.profile._id,
                },
            }, {
                new: true,
            }
        );
        res.json(nft);
    } catch (err) {
        _c59‍.g.console.log(err);
    }
};

       const unlikeNft = async(req, res) => {
    try {
        const nft = await Nft.findByIdAndUpdate(
            req.body._id, {
                $pull: {
                    unlikes: req.profile._id,
                },
            }, { new: true }
        );
        res.json(nft);
    } catch (err) {
        _c59‍.g.console.log(err);
    }
};

       const usersNft = async(req, res) => {
    try {
        const nft = await Nft.find();
        // .populate("createdBy", "name ipfs")
        //     .sort({ createdAt: -1 })
        //     .limit(10);
        res.json(nft);
    } catch (err) {
        _c59‍.g.console.log(err);
    }
};