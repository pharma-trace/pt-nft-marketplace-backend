let mongoose;_063‍.x([["default",()=>_063‍.o]]);_063‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const { Schema } = mongoose;

const profileSchema = new Schema(
  {
    name: {
      type: String,
      //   required: true,
      trim: true,
    },
    email: {
      type: String,
      trim: true,
      //   required: true,
      // unique: true,
    },
    about: {},
    isColour: {
      type: Boolean,
      required: true,
    },
    image: String,
    banner: String,
    following: [{ type: Schema.ObjectId, ref: "User" }],
    followers: [{ type: Schema.ObjectId, ref: "User" }],
    wallet: {
      type: String,
    },
  },
  { timestamps: true }
);

_063‍.d(mongoose.model("Profile", profileSchema));
