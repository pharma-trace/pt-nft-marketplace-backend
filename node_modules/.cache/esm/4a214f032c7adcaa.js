let express,mongoose,cors,readdirSync;_2c7‍.w("express",[["default",["express"],function(v){express=v}]]);_2c7‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_2c7‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_2c7‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);



//bridge-between-routes-controllers


require("dotenv").config();

const app = express();

mongoose
    .connect(process.env.DB, {
        useNewUrlParser: true,
        useUnifiedTopology: true,
    })
    .then(() => console.log("PharmaTrace Database Connected"))
    .catch((err) => _2c7‍.g.console.log(err));

app.use(express.json());
app.use(express.urlencoded({ extended: true }));
app.use(
    cors({
        origin: "http://localhost:3000",
    })
);

readdirSync("./routes").map((r) => app.use("/api", require(`./routes/${r}`)));

const port = process.env.PORT || 8000;

app.listen(port, () =>
    console.log(`PharmaTrace Server Connected at Port ${port}`)
);